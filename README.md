# geo_json_v2
Python CLI tool that uses the Geoapify Geocoding API to convert user-entered addresses into structured location data. It sends HTTP requests, parses JSON responses, extracts key geographic details, handles errors safely, and supports Unicode for multilingual place names.
